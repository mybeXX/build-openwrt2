name: build x86/64 openwrt

on:
  workflow_dispatch:
    inputs:
      source:
        description: '选择要编译的源码'
        required: true
        default: 'immwrt'
        type: choice
        options: ['immwrt', 'lean']

      version:
        description: '输入版本号 (例如: 24.10.4, v23.05.7, 或 master)'
        required: true
        default: '24.10.4'
        type: string

      target:
        description: '选择机型'
        required: true
        default: 'x86-64'
        type: choice
        options: ['x86-64']

      ip_address:
        description: '设置默认IP地址'
        required: false
        default: '10.0.0.1'

      partsize:
        description: '设置rootfs大小 (MB)'
        required: false
        default: '500'

      release:
        description: '上传到 Releases'
        required: false
        default: true
        type: boolean

jobs:
  build:
    name: build ${{inputs.target}} ${{inputs.source}}
    runs-on: ubuntu-24.04

    env:
      IP_ADDRESS: ${{inputs.ip_address}}
      PART_SIZE: ${{inputs.partsize}}
      DIY_SCRIPT: ${{inputs.source}}.sh
      REPO_BRANCH: ${{inputs.version}}
      TZ: Asia/Shanghai

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Initialization Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo -E apt -yqq update
          sudo -E apt -yqq install $(curl -fsSL is.gd/depends_ubuntu_2404)
          sudo timedatectl set-timezone "$TZ"
          sudo mkdir -p /workdir
          sudo chown runner:runner /workdir

      - name: Clone Source Code
        run: |
          if [ "${{ inputs.source }}" == "immwrt" ]; then
            REPO_URL="https://github.com/immortalwrt/immortalwrt"
          else
            REPO_URL="https://github.com/coolsnowwolf/lede"
          fi
          git clone --depth 1 --tags -b $REPO_BRANCH $REPO_URL /workdir/openwrt
          echo "OPENWRT_PATH=/workdir/openwrt" >> $GITHUB_ENV
          echo "REPO_URL=$REPO_URL" >> $GITHUB_ENV

      - name: Get Commit Data
        run: |
          cd $OPENWRT_PATH
          echo "COMMIT_AUTHOR=$(git log -1 --pretty=format:'%an')" >> $GITHUB_ENV
          echo "COMMIT_DATE=$(git log -1 --pretty=format:'%ad')" >> $GITHUB_ENV
          echo "COMMIT_MESSAGE=$(git log -1 --pretty=format:'%s')" >> $GITHUB_ENV
          echo "COMMIT_HASH=$(git log -1 --pretty=format:'%h')" >> $GITHUB_ENV
          echo "KERNEL_VERSION=$(grep -oP '(?<=KERNEL_PATCHVER:=).*' target/linux/x86/Makefile || echo "Unknown")" >> $GITHUB_ENV

      - name: Load Custom Configuration
        run: |
          CONFIG_FILE="configs/${{inputs.target}}-${{inputs.source}}.config"
          [ -e $CONFIG_FILE ] && cp $CONFIG_FILE $OPENWRT_PATH/.config
          
          chmod +x $DIY_SCRIPT && ./$DIY_SCRIPT
          
          cd $OPENWRT_PATH
          # 自动修改 rootfs 空间
          sed -i "s/CONFIG_TARGET_ROOTFS_PARTSIZE=.*/CONFIG_TARGET_ROOTFS_PARTSIZE=$PART_SIZE/" .config
          # 自动修改默认 IP
          sed -i "s/192.168.1.1/$IP_ADDRESS/g" package/base-files/files/bin/config_generate

      - name: Compile Firmware
        id: compile
        run: |
          cd $OPENWRT_PATH
          make defconfig
          make -j$(nproc) || make -j1 V=s
          echo "DATE=$(date +"%Y.%m.%d-%H:%M")" >> $GITHUB_ENV
          echo "status=success" >> $GITHUB_OUTPUT

      - name: Upload Firmware To Release
        if: steps.compile.outputs.status == 'success' && inputs.release == 'true'
        uses: ncipollo/release-action@main
        with:
          name: ${{ inputs.target }}-${{ env.REPO_BRANCH }}-${{ env.DATE }}
          tag: ${{ inputs.target }}-${{ env.REPO_BRANCH }}-${{ env.DATE }}
          token: ${{ secrets.GITHUB_TOKEN }}
          artifacts: ${{ env.OPENWRT_PATH }}/bin/targets/${{ inputs.target }}/*/*
          body: |
            **这是基于 ${{ env.REPO_BRANCH }} 版本编译的固件**
            ### 📒 固件信息
            - 💻 平台架构: ${{ inputs.target }}
            - ⚽ 固件源码: ${{ env.REPO_URL }}
            - 💝 源码分支: ${{ env.REPO_BRANCH }}
            - 🚀 内核版本: ${{ env.KERNEL_VERSION }}
            - 🌐 默认地址: ${{ env.IP_ADDRESS }}
            - 🔑 默认密码: password
            ### 🧊 源码最新记录
            - **作者**: ${{ env.COMMIT_AUTHOR }}
            - **日期**: ${{ env.COMMIT_DATE }}
            - **摘要**: ${{ env.COMMIT_MESSAGE }}
            - **哈希**: ${{ env.COMMIT_HASH }}
